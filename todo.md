My personal list of todos for this project:

- Add logic for renaming files:
  - X make sure you can change the name via keyboard shortcuts
- Dockerize APP

- Vite & express both need to live on their own port. There should probably be a .env file along with a docker compose that can be used to change environment varriables and credentials
- The app should have a default account
- Create an admin panel for that account to manage users
- Fix issue when using /
